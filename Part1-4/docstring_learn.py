def add(x=0, y=0):
    """
    Returns x + y.

    Parameters
    ----------
    x : int
        足される数
    y : int
        足す数

    Returns
    ----------
    x + y : int
        xとyの和
    """
    return x + y

x = add(3,4)
print(x)